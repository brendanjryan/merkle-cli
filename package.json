{
  "name": "merkle-cli",
  "version": "0.0.1",
  "description": "An extensible command line tool for generating merkle trees and proofs.",
  "main": "./bin/generate.ts",
  "scripts": {
    "build": "npx tsc",
    "generate": "ts-node -T ./bin/generate.ts",
    "test": "jest -c jest.config.ts"
  },
  "author": "@brendanjryan",
  "license": "ISC",
  "dependencies": {
    "circomlibjs": "^0.1.7",
    "commander": "^11.0.0",
    "ts-node": "^10.9.1"
  },
  "devDependencies": {
    "@commander-js/extra-typings": "^11.0.0",
    "jest": "^29.6.1",
    "ts-jest": "^29.1.1",
    "test-jest": "^1.0.1",
    "@types/jest": "^29.5.2"
  }
}
